diff --git CMakeLists.txt CMakeLists.txt
index 35d9ada8..c06bc8e6 100644
--- CMakeLists.txt
+++ CMakeLists.txt
@@ -11,10 +11,10 @@ set(API_VERSION ${PROJECT_VERSION})
 
 list(APPEND CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR}/cmake)
 
-option(ROBOT_MODEL_RBDL "Also build the RBDL-based robot model, by default only pinocchio is built" OFF)
+option(ROBOT_MODEL_RBDL "Also build the RBDL-based robot model, by default only pinocchio is built" ON)
 option(ROBOT_MODEL_HYRODYN "Also build the HyRoDyn-based robot model, by default only pinocchio is built" OFF)
 option(SOLVER_PROXQP "Build the ProxQP-based solver, by default only hls and qpoases are built" OFF)
-option(SOLVER_EIQUADPROG "Build the Eiquadprog-based solver, by default only hls and qpoases are built" OFF)
+option(SOLVER_EIQUADPROG "Build the Eiquadprog-based solver, by default only hls and qpoases are built" ON)
 option(SOLVER_QPSWIFT "Build the QPSwift-based solver, by default only hls and qpoases are built" OFF)
 option(SOLVER_OSQP "Build the OSQP-based solver, by default only hls and qpoases are built" OFF)
 
diff --git src/solvers/eiquadprog/CMakeLists.txt src/solvers/eiquadprog/CMakeLists.txt
index c58a7129..fb30d009 100644
--- src/solvers/eiquadprog/CMakeLists.txt
+++ src/solvers/eiquadprog/CMakeLists.txt
@@ -2,17 +2,22 @@ SET(TARGET_NAME wbc-solvers-eiquadprog)
 
 pkg_search_module(eiquadprog REQUIRED IMPORTED_TARGET eiquadprog)
 
+pkg_check_modules(EIQUADPROG REQUIRED eiquadprog)
+include_directories(${EIQUADPROG_INCLUDE_DIRS})
+link_directories(${EIQUADPROG_LIBRARY_DIRS})
+add_definitions(${EIQUADPROG_CFLAGS_OTHER})  #flags excluding the ones with -I
+
 set(SOURCES EiquadprogSolver.cpp)
 set(HEADERS EiquadprogSolver.hpp)
 
-list(APPEND PKGCONFIG_REQUIRES eiquadprog)
+#list(APPEND PKGCONFIG_REQUIRES eiquadprog)
 list(APPEND PKGCONFIG_REQUIRES wbc-core)
 string (REPLACE ";" " " PKGCONFIG_REQUIRES "${PKGCONFIG_REQUIRES}")
 
 add_library(${TARGET_NAME} SHARED ${SOURCES} ${HEADERS})
 target_link_libraries(${TARGET_NAME} PUBLIC
                       wbc-core
-                      PkgConfig::eiquadprog)
+                      ${EIQUADPROG_LIBRARIES})
 
 set_target_properties(${TARGET_NAME} PROPERTIES
        VERSION ${PROJECT_VERSION}
